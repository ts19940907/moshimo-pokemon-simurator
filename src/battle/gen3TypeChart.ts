/**
 * Gen3 (ADV) type effectiveness — identical to Gen2 (GSC) until Fairy (Gen6).
 * Re-exported so call sites can be explicit about Gen3 rules.
 */
export {
  gen2TypeEffectiveness as gen3TypeEffectiveness,
} from "./gen2TypeChart";
